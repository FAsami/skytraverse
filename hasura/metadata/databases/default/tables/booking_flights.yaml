table:
  name: flights
  schema: booking
object_relationships:
  - name: paymentMethod
    using:
      manual_configuration:
        column_mapping:
          paymentMethodId: id
        insertion_order: null
        remote_table:
          name: availablePaymentMethods
          schema: payment
