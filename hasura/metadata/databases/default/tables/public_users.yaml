table:
  name: users
  schema: public
object_relationships:
  - name: default
    using:
      manual_configuration:
        column_mapping:
          id: userId
        insertion_order: null
        remote_table:
          name: details
          schema: customer
array_relationships:
  - name: accounts
    using:
      foreign_key_constraint_on:
        column: userId
        table:
          name: accounts
          schema: public
  - name: sessions
    using:
      foreign_key_constraint_on:
        column: userId
        table:
          name: sessions
          schema: public
